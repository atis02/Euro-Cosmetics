interface images {
  image?: string;
  productStatus?: string;
  title: string;
  sellPrice?: number;
  discountPrice?: number;
  category?: string;
  stock: number;
  feedBackNumb: number;
  articule: string;
  desc: string;
}
export interface Product {
  product: images;
}
